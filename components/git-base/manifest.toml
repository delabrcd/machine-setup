name = "git-base"
description = "Non-identity git config: defaultBranch, pull.rebase, signing format"
supported = ["linux-ubuntu", "linux-fedora", "linux-arch", "wsl", "windows"]
depends_on = ["packages"]
per_identity = false
