name = "git-identity"
description = "Per-identity git config (user.name/email, signingKey, includeIf for non-default identities)"
supported = ["linux-ubuntu", "linux-fedora", "linux-arch", "wsl", "windows"]
depends_on = ["git-base"]
per_identity = true
