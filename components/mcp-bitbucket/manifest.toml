name = "mcp-bitbucket"
description = "Enable the Bitbucket MCP (aashari/mcp-server-atlassian-bitbucket) for Claude Code"
supported = ["linux-ubuntu", "linux-fedora", "linux-arch", "wsl", "windows"]
# Signal-only. chezmoi handles registration + BW secret extraction; nvm is
# needed at MCP-call time for `npx`.
depends_on = ["claude-code", "chezmoi", "nvm"]
per_identity = false
