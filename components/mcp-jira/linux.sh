#!/usr/bin/env bash
# Signal-only component. chezmoi reads the plan and registers the jira MCP
# itself (using bitwardenFields for JIRA_URL + JIRA_PERSONAL_TOKEN).
log "jira MCP enabled — registration handled by chezmoi"
