name = "mcp-jira"
description = "Enable the Jira MCP (sooperset/mcp-atlassian via uvx) for Claude Code"
supported = ["linux-ubuntu", "linux-fedora", "linux-arch", "wsl", "windows"]
# Signal-only. chezmoi handles registration + BW secret extraction; uv is
# needed at MCP-call time for `uvx`.
depends_on = ["claude-code", "chezmoi", "uv"]
per_identity = false
